To use the App follow the instructions.

Inside the root directory of API (FH_api) run these two commands,

npm install
npm run server

Inside the root directory of Frontend(frontend) run these three commands,

npm install -g gatsby-cli   (note: If you installed gatsby before dont need to run this command)
npm install
gatsby develop
